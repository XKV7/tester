# DECISIONS

명세에서 모호했던 부분을 구현하면서 확정한 결정 (한 줄씩).

- 첫 입력은 `hitTime[1]`에서 판정한다. `hitTime[0] = offset`은 행성이 타일 0에서 공전을 시작하는 시각이다.
- 타일 수 = `path.length + 1`, 마지막 타일이 도착 타일. "32타일" 데모는 path 31개.
- Midspin은 `path[i] = path[i-1] + 180`(되돌아가는 방향)일 때만 허용한다. 그러면 다음 타일이 이전 축 위치와 겹쳐 θ=0이 기하적으로 성립한다. 에디터에서 Midspin을 추가하면 각도를 자동 보정.
- Midspin 타일 통과 시 별도 판정은 기록하지 않는다 (직전 입력 하나로 두 타일 진행).
- Pause/Hold의 추가 공전은 `round(박/2)` 바퀴(최소 1바퀴)로 돌고, 각속도를 조정해 목표 각도에 정확히 도착한다. 짝수 박이면 기본 각속도와 같다.
- Hold 타일: 타일 i에 도착하는 입력을 누른 채 유지, `hitTime[i] + beats × 60/BPM`에 떼야 한다. 떼기도 같은 판정 창으로 판정하며, 창보다 일찍 떼거나 늦게 떼면 실패.
- "너무 빠름" 하한(해당 구간 θ 시간의 절반)은 Pause/Hold를 포함한 전체 구간 시간의 절반으로 계산한다. 그보다 이른 입력은 무시.
- 판정 창 배율은 창 상한 계산 전에 적용한다: `min(x × 배율, beatMs × 0.25)`.
- 판정 오차·창은 실제 시간(ms) 기준 — `pitch`가 1이 아니면 곡 시간 오차를 pitch로 나눈다.
- 정확도 가중치: 완벽 1, 약간 0.75, 빠름·느림 0.4, 너무 빠름 0 (별도 항목으로 누적되어 정확도를 깎음).
- 완벽 클리어 = 모든 판정이 완벽. 무결점 클리어 = 실패·체크포인트 사용·너무 빠름 없이 처음부터 한 번에 클리어. 자동 플레이는 배지·기록 없음.
- 체크포인트 재개 시 해당 타일 이후의 판정 기록은 지우고 이전 기록은 유지한다.
- 출력 지연(`outputLatency`)은 곡 시간 단위로 빼기 위해 `× pitch` 한다. 입력 오프셋·화면 오프셋도 동일.
- 입력 오프셋 양수 = 늦게 누르는 경향 보정(판정 시각에서 뺌). 화면 오프셋 양수 = 화면을 앞당겨 그림.
- 화면 보정은 입력 오프셋을 먼저 뺀 탭 오차의 평균을 화면 오프셋으로 제안한다. 보정 평균은 중앙값에서 먼 4개를 버린 12개로 계산.
- 연출 이벤트 duration(박)은 이벤트가 시작되는 타일의 BPM으로 초 변환한다 (도중 속도 변경은 반영하지 않음).
- MoveTrack `offset`/`rotation`/`opacity`는 원래 위치 기준 절대 목표값이다 (누적이 아님). RecolorTrack/MoveTrack의 `from`/`to`는 포함 범위.
- Camera `offset`은 수학 좌표(y 위쪽) 월드 단위, `rotation`은 도(반시계 양수가 아니라 화면 기준 시계 방향 양수).
- Flash는 `opacity`에서 0으로 `duration`동안 감쇠 (duration 없으면 0.25초).
- Background `image`는 패키지 안의 파일 이름.
- 명세 확장: 튜토리얼 안내 문구용 `Text` 이벤트(`{ floor, type: "Text", text }`)를 추가했다.
- 음원이 없거나 디코딩에 실패하면 레벨 hitTime에 맞춘 합성 비트 트랙으로 대체한다 (경고 표시).
- 불러온 레벨은 세션 동안만 목록에 유지된다. 설정·최고 기록·에디터 작업 중 레벨 JSON은 localStorage에 저장.
- 에디터의 "Shift+방향키" = 선택 타일의 나가는 각도 ±15° (←/↑ 반시계, →/↓ 시계). 방향키 단독은 선택 이동. T = 선택 타일 Twirl 토글.
- 에디터 타일 추가는 선택 타일 "뒤에 삽입" — 중간 타일이면 이후 타일이 밀리고 이벤트 floor도 함께 이동.
- 녹화 모드: 첫 간격은 선택 타일의 hitTime부터 잰다. 간격 → 박 → θ를 15° 스냅(15°~360°). BPM은 선택 타일의 BPM 고정.
  - 한 방향 회전: 현재 회전 방향 그대로. 지그재그: 꺾임마다 Twirl을 넣어 좌·우를 번갈아. 직진 우선: θ가 180±30°면 직진으로 스냅.
- 자동 생성 보조(직진 채우기)에 BPM을 주면 선택 타일에 SetSpeed(bpm)를 넣는다.
- 일시정지 해제 시 1.5초(3카운트) 뒤 재개. 탭이 숨겨지면 자동 일시정지.
- 과부하 시각 기준은 입력 이벤트의 실제 시각(`event.timeStamp`).
- 자동 타격음은 0.35초 앞까지 오디오 시계에 예약하고, 실패·정지 시 예약 버스를 끊어 취소한다.
- 렌더링 부드러운 시계: `audioCtx.currentTime`을 `performance.now()`로 외삽, 차이가 20ms 이상이면 스냅, 아니면 8%씩 수렴(단조 증가 유지).
- 카메라 추적·입자 애니메이션은 프레임 간격을 쓰지만(시각 보간 전용), 게임 진행·판정·연출 이벤트 시각은 전부 마스터 클럭에서 읽는다.
- 컬링: 카메라 중심에서 화면 대각선 반지름 + 1타일 밖의 타일은 숨기고, 타일 그래픽은 처음 화면에 들어올 때 생성(띠 모양 GraphicsContext는 각도 조합별로 공유).
- TypeScript 7(네이티브) 대신 5.x를 사용 (Vite/Vitest 생태계 호환).
- zip 처리는 fflate 사용.
- 데모 레벨 BPM을 낮춤 (튜토리얼 80, 스윙 96, 셋잇단 88, 쉼표와 홀드 84, 가속 110→150). 모든 데모는 입력 간격 200ms 이상, 트랙 자기 겹침 없음을 테스트로 보장.
- 데모 레벨은 각도 대신 박 수로 작성 (`LevelBuilder`): 회전 방향 기준 1박 = 직진, b박 = 180(1−b)° 꺾기.
- 플레이 속도(×0.5~×1.5)는 레벨 pitch에 곱해 곡·판정 시각을 함께 바꾼다. 판정 창은 실제 ms 기준이라 느리게 하면 쉬워진다. ×1이 아니면 최고 기록·배지를 남기지 않는다.
- BPM 자동 추정: 10ms 로그 에너지 증가량 → BPM 후보(60~200, 120 근처 약한 선호)별 박 그리드 점수 → 최고 후보 ±0.6BPM·위상 ±15ms 세밀화. 적용 시 첫 박은 카운트다운 여유를 위해 1초 이후의 박으로 옮긴다.
- 포함 레벨: `public/levels/index.json`(폴더 이름 배열)을 시작 시 불러온다. 없거나 실패하면 조용히 건너뛰고 콘솔 경고만 남긴다.
- 자동 레벨 생성(`core/autochart.ts`): 반박 격자에서 onset 세기가 박/반박별 백분위 기준(쉬움 30%/90%, 보통 20%/72%, 어려움 5%/45%) 이상인 지점을 타격으로 고른다. 1.5박 초과 빈 곳은 1박씩 채우고(2박 U턴 없음), 200ms 미만 간격은 버린다. BPM은 곡 전체 고정.
- 자동 생성은 체크포인트·트랙 색 변경을 넣지 않는다 (사용자 요청). 이벤트는 겹침 방지용 Twirl뿐.
- 자동 레이아웃: 꺾일 때마다 두 회전 방향을 12칸 앞까지 시뮬레이션해 더 오래 겹치지 않는 쪽을 고르고, 방향이 바뀌면 Twirl을 넣는다.
- 에디터에서 빈 레벨(타일 5개 이하, 이벤트 없음)에 음원을 고르면 자동으로 '보통' 난이도로 생성한다. 그 외에는 버튼으로만 생성(확인 후 교체, 실행 취소 가능).
- 음악 자동 생성(`audio/songgen.ts`): 규칙 기반 작곡(분위기별 조성·코드 진행·드럼/베이스 패턴·2마디 모티프 반복 멜로디) + 샘플 단위 합성. seed가 같으면 같은 곡. 레벨은 음표에서 바로 만든다(쉬움 킥·스네어 / 보통 + 멜로디 / 어려움 + 베이스), 빈 곳 채움은 다른 악기 음표 위치 우선.
- 레벨 생성 공통부(`levelFromHits`)를 음원 분석과 음악 생성이 함께 쓴다. 채움 후보(fill)가 있으면 prev+1에 가장 가까운 후보를 고른다.
- 생성한 곡은 16비트 모노 WAV로 패키지에 넣는다 (zip 내보내기 호환).
- 에디터 줌: 휠·트랙패드 핀치(ctrl+wheel, 더 민감), 두 손가락 핀치(가운데 지점 고정 + 이동), +/−/0 키, 화면 버튼. 배율 3%~600%. 카메라 가운데 맞춤은 화면 전체가 아니라 편집 영역(패널 제외) 기준.
- 에디터 방향 패드: 화살표 = 새 타일이 이어지는 방향(키보드 D/W/A/S/E/Q/Z/C와 같음), 가운데 = 삭제. 터치 기기(hover:none + pointer:coarse)나 폭 760px 미만이면 기본 표시, 설정은 localStorage에 기억. 모바일에서는 위쪽 버튼 막대를 한 줄 가로 스크롤로 바꿔 편집 영역을 넓힘.
